{
  "name": "backend",
  "version": "1.0.0",
  "dependencies": {
    "@useoptic/cli": "^8.6.2",
    "cross-env": "5.0.5",
    "frontend": "1.0.0"
  },
  "scripts": {
    "start": "rails s",
    "test": "rails test",
    "deploy": "git subtree push --prefix backend heroku main"
  }
}
