json.extract! step,
              :id,
              :name,
              :status,
              :description,
              :created_at,
              :updated_at,
              :next_step_id,
              :workflow_run_id,
              :user,
              :authorization
# json.url step_url(step, format: :json)
